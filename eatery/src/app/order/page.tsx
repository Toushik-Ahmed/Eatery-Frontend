import Cards from '@/components/posComponent/Cards'
import React from 'react'

type Props = {}

const page = (props: Props) => {
  return (
    <div>
        <Cards />
    </div>
  )
}

export default page